import type React from "react";

// Types for EdlExportBox component (Caixa de texto exportável)
export interface CaixaDeTextoExportavelProps {
  texto: string;
  nomeArquivo?: string;
  titulo?: string;
}

